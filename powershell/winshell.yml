---
- name: test win_command
  hosts: all
 
  tasks:
 
  - name: run powershell
    win_command: powershell.exe
    args:
      stdin: ls
    register: shellresults
  
  - set_fact:
      vm: "{{ shellresults.stdout | from_json }}"
